import App from "./src/App.svelte";

const target = document.getElementById("root");

const app = new App({ target });

export default app;
import App from "./src/app.svelte";

const target = document.getElementById("root");

const app = new App({ target });

export default app;